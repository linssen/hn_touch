<div class="article">

    <a href="{{ url }}">
        <h4>{{ title }}</h4>
        <p class="subtext">
            {{ points }} points by {{ submitter }}
            {{ comment_count }} comments
        </p>
        {{#if mobalised }}{{ mobalised }}{{/if}}
    </a>

</div>